# Test Directory

This directory contains all the test files for the frontend application. 

## Running Tests

To run the tests, use the following command:

```bash
npm test
```

Make sure to add your test files here following the naming convention `*.test.ts`. 
